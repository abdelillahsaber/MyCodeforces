#include<iostream>
 
using namespace std;
 
int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int t;
    cin >> t;
    while(t--) {
        int rating;
        cin >> rating;
        int x;
        if (rating >= 1900)
        {
            x = 1;
        } else if (rating >= 1600)
    {
        x = 2;
    } else if (rating >= 1400)
{
    x = 3;
} else {
x = 4;
}
cout << "Division " << x << endl;
}
return 0;
}