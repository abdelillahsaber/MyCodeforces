#include<iostream>
#include<algorithm>
 
using namespace std;
 
void solve() {
    int n,k;
    cin >> n >> k;
    bool found = false;
    for (int i = 0; i < n; i++) {
        int x;
        cin >> x;
        if (x == k) found = true;
    }
    if (found) cout << "yes" << endl;
    else cout << "no" << endl;
}
 
int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int t;
    cin >> t;
    while (t--) solve();
    return 0;
}